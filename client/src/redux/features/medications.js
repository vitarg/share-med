const initialState = {
  item: null,
  signinUp: false,
  signinIn: false,
  token: null,
};

export default function medications(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
